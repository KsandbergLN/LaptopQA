# Laptop QA

This private repository contains the two supported Laptop QA applications:

- Windows app: the repository root, built with `Build-LaptopQAIteration.ps1`.
- macOS companion: `macos/`, built with `macos/Build-MacRelease.ps1`.

The repository root is the canonical source. Generated build output, runtime data, logs, and release packages are intentionally ignored.
